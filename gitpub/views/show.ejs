<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GitPub</title>
    <style>
        body {
            background-color: steelblue;
            color: blanchedalmond;
            text-align: center;
        }
        li::first-letter {
            text-transform: capitalize;
        }
        img {
            max-width: 500px;
            max-height: 500px;
        }
        h3 {
            font-size: 50px;
        }
        h1, h2 {
            text-transform: capitalize;
        }

        h1 {
            background-color: crimson;
        }

        @media only screen and (max-width: 600px) {
            img {
            max-width: 300px;
            max-height: 300px;
        }
    
        }
    </style>
</head>
<body>
    <h1>At foo bar</h1>
       <h2> <%= `${drink.name}` %> </h2> 
       <img src="<%= `${drink.image}.png` %>" >
       <h3> $<%= `${drink.price}` %> </h3>
       <h6><a href="/drinks">GO BACK TO LIST OF FRUITS</a></h6>
</body>
</html>